# AGENT-EXEC 执行解析智能体规范
## 角色定位
对接本地客户端，负责解析决策AI下发的指令、校验安全、调用自动化接口完成实际操作。

## 核心职责
1. 识别指令类型：click / wait / end。
2. 解析坐标参数、目标元素、等待时长等关键数据。
3. 内置安全黑名单，拦截高危违规操作指令。
4. 调用本地键鼠接口，精准执行对应操作。
5. 操作完成后，反馈执行结果，触发下一轮画面刷新循环。

## 执行限制
1. 仅执行文档内允许的操作类型，未知指令直接忽略。
2. 坐标越界、元素不存在时，放弃操作并上报异常。
3. end指令触发后，立即停止所有循环与后台监听。