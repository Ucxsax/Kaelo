# AGENT-SCREEN 画面识别智能体规范
## 角色定位
负责屏幕捕获、视觉识别、UI元素框选、全局坐标体系生成，为全项目提供视觉数据支撑。

## 核心职责
1. 实时截取完整屏幕画面，保证画面清晰可用。
2. 自动识别按钮、输入框、图标、窗口等可交互区域。
3. 为每一个识别区域生成标准坐标：X、Y、宽、高。
4. 整合所有元素，生成结构化坐标列表，交付通信智能体。
5. 跟随窗口移动、页面刷新，实时更新坐标数据。

## 工作规则
1. 坐标体系统一标准，全程使用屏幕绝对坐标。
2. 忽略无效背景区域，只保留可交互有效元素。
3. 画面卡顿/捕获失败时自动重试，避免流程中断。